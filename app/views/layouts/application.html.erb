<!DOCTYPE html>
<html>
  <head>
    <title>LibraApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-dark mb-0">
        <% if @current_user %>
          <a class="navbar-brand text-light mr-2" href="/posts/index">Libra</a>
        <% else %>
          <a class="navbar-brand text-light" href="/">Libra</a>
        <% end %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <nav class="navbar navbar-dark bg-dark custom-hamburger">
            <span class="navbar-toggler-icon"></span>
          </nav>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">

            <% if @current_user %>
              <li class="nav-item">
                <%= link_to(@current_user.name, "/users/#{@current_user.id}", {method: "get", class: "nav-link text-light"}) %>
              </li>
              <li class="nav-item">
                <a class="nav-link text-light" href="/posts/index">投稿一覧</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-light" href="/posts/new">新規投稿</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-light" href="/users/index">ユーザー</a>
              </li>
              <li class="nav-item">
                <%= link_to("ログアウト", "/logout", {method: "post", class: "nav-link text-light"}) %>
              </li>
            <% else %>
              <li class="nav-item">
                <a class="nav-link text-light" href="/signup">新規登録</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-light" href="/login">ログイン</a>
              </li>
            <% end %>
          </ul>
        </div>
      </nav>
    </header>

    <% if flash.now[:notice] %>
      <div class="flash">
        <%= flash.now[:notice] %>
      </div>
    <% end %>

    <%= yield %>
  </body>

  <footer class="text-center text-muted py-4 mt-4">
    Copyright 2020 Libra
  </footer>
</html>
